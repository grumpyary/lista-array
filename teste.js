/* const prompt = require('prompt-sync')()

// Solicita ao usuário um valor N
const valorN = parseInt(prompt("Digite um valor N para a sequência de Fibonacci:"));

// Declaração dos primeiros dois termos da sequência de Fibonacci
let termo1 = 0;
let termo2 = 1;

// Variável para armazenar os termos da sequência de Fibonacci
let sequenciaFibonacci = [];

// Adiciona os primeiros dois termos à sequência
sequenciaFibonacci.push(termo1);
sequenciaFibonacci.push(termo2);

// Gera a sequência de Fibonacci até o N-ésimo termo
for (let i = 2; i < valorN; i++) {
  const novoTermo = termo1 + termo2;
  sequenciaFibonacci.push(novoTermo);

  // Atualiza os valores dos termos anteriores
  termo1 = termo2;
  termo2 = novoTermo;
}

// Imprime a sequência de Fibonacci
console.log("A sequência de Fibonacci até o", valorN + "º", "termo é:", sequenciaFibonacci.join(", "));

*/

let balance = parseInt(readLine(), 10);
let withdraw = parseInt(readLine(), 10);
let -=remai 